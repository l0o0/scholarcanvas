# Keep the plugin and its independently reusable development packages explicit.
packages:
  - "."
  - "packages/whiteboard"
  - "packages/fake-zotero"

# Settings (pnpm 10+)
blockExoticSubdeps: false

allowBuilds:
  "@swc/core": true
  es5-ext: true
  esbuild: true
