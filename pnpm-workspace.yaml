# Single-package project; keep workspace root explicit for pnpm.
packages:
  - "."
  - "packages/whiteboard"

# Settings (pnpm 10+)
blockExoticSubdeps: false

allowBuilds:
  "@swc/core": true
  es5-ext: true
  esbuild: true
